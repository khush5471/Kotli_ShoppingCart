<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.kotlin_shoppingcart">
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />



    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"

        android:theme="@style/AppTheme">
        <activity android:name=".views.activities.MainActivity"
            android:theme="@style/coustomTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".views.activities.LoginActivity"
            android:windowSoftInputMode="adjustPan"
            android:theme="@style/coustomTheme"/>
        <activity android:name=".views.activities.HolderActivity"
            android:theme="@style/coustomTheme"
            />
        <activity android:name=".views.activities.TransactionActivity" android:theme="@style/coustomTheme"/>
    </application>

</manifest>